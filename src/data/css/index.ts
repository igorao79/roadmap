import { LanguageData } from '../types';
import { cssCards } from './cards';
import { cssContent } from './content';

export const cssData: LanguageData = {
  name: 'CSS',
  color: '#1572B6',
  cards: cssCards,
  content: cssContent
};




