import { LanguageData } from '../types';
import { htmlCards } from './cards';
import { htmlContent } from './content';

export const htmlData: LanguageData = {
  name: 'HTML',
  color: '#E34F26',
  cards: htmlCards,
  content: htmlContent
};

